import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.mrteam',
  appName: 'mr-team',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
